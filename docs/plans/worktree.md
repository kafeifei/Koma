# Worktree 实施与验收

状态：已完成。2026-09-07 完成选择器、占用、文件保留、管理、合回及 V2 生命周期实施，完成下列验收并合入本地 `dev`。候选包已验证，未安装或重启用户应用。

## 已完成的基础能力

- 新建 Worktree、选择创建起点，以及首次发送前等待目录准备完成。
- 手动归档、保存 Git 可表达的暂存／未暂存／普通未跟踪文件状态、回收目录、恢复任务。
- 删除确认，以及受管理的独占目录、分支和保存引用的清理。
- V1 Session／子任务的运行占用保护、失败重试及生命周期恢复。
- 桌面 Node 后端兼容性与包内创建／归档／恢复／删除验证。

定时自动回收不在计划中，沿用用户已明确的决定。

## 优先项：按 Sandy 桌面合同修正选择器

### 已确认的问题

实施前的选择器把 `main`、`create` 和已有目录路径混在同一个值里，将所有非 `main` 状态都显示为 Worktree 已勾选。

以用户截图中的新建任务 `kind-wolf` 为例：

- 当前目标是已有 Worktree，勾选状态却容易被理解为将新建独立 Worktree。
- 菜单仍显示“基础分支”，但提交路径只有 `create` 才使用该值；在已有目录状态下选择分支不生效。
- 取消勾选会导航到项目根目录的输入页，再次勾选则变成创建另一个 Worktree；这不是单纯切换隔离模式。
- “主分支”实际表示项目根目录，操作没有执行 Git checkout，文案混淆了目录与分支。
- “工作区…”子菜单和已有 Worktree 列表来自另一套目录选择语义，不符合 Sandy 桌面的独立分支控件与 Worktree 复选框结构。

涉及边界：[选择器](../../packages/app/src/components/prompt-workspace-selector.tsx)、[新任务工作区控制器](../../packages/app/src/pages/new-session/new-session-workspace-controller.ts)、[首次提交](../../packages/app/src/components/prompt-input/submit.ts)。

### Sandy 参考与需要明确的差异

参考当前 Sandy Agent Host 桌面路径，不使用 legacy Copilot picker 或手机统一面板代替桌面合同：

- 顺序为「项目／文件夹」「分支」「Worktree 复选框」，分支菜单只负责分支选择。
- Git 项目首次无偏好时默认开启 Worktree，随后沿用用户上次开关选择；分支不作为跨任务通用偏好记忆。
- Worktree 模式选择的分支是新建隔离目录的起点，不表示复用占用该分支的已有 Worktree。
- 已创建任务的目录和隔离配置是已确定的事实，不能由新建控件原地改动。
- Sandy 当前 folder 模式的 Branch 虽可选择，但只写配置，不执行 checkout／switch。这一冗余行为需要在 OpenCode 中明确消除，不能把它描述成真实切换分支。

参考源码：Sandy 的 `agentHostSessionConfigPicker.ts`、`baseAgentHostSessionsProvider.ts`、`platform/agentHost/node/shared/worktreeIsolation.ts`；本次分析未修改 Sandy。

### 已实现的行为合同

1. 独立表达来源目录、是否创建隔离 Worktree、创建起点分支。Worktree 开关不承担目录导航。
2. 开启时允许选择起点分支，首次发送才创建本任务的新 Worktree；切换开关不提前创建目录或分支。
3. 关闭时使用所选目录及其实际分支，显示事实，避免提供不生效的分支选择；不自动切回项目根目录或所谓“主分支”。
4. 已有 Worktree 作为可选目录处理，不再借复选框状态表示复用它。具体目录入口应与项目／目录选择保持一致。
5. 保留最后一次有效的隔离偏好；非 Git、无提交、加载和失败状态有明确禁用／恢复行为。
6. 沿用[输入保留合同](input-retention.md)：显式切换目录才打开目标目录自己的输入；切换隔离模式不搬运或替换当前输入。待创建 Worktree 在获得真实路径前，输入仍属于来源目录。

### 实施和验收顺序

- [x] 先提供桌面控件预览，对齐已有目录入口和关闭 Worktree 时的分支展示。
- [x] 在所属控制器拆清状态，再调整呈现；保留现有 SDK、创建等待、防重复提交和后端生命周期能力。
- [x] 验证主目录与已有 Worktree 下的新建任务，开关往返不跳目录、不丢文字／附件／上下文。
- [x] 验证分支选择的实际效果：开启时从所选分支创建新目录，关闭时继续使用原目录及实际分支，无无效选项。
- [x] 验证新建、关闭重开、刷新、切换项目后的隔离偏好，以及分支选择不会错误沿用到另一项目。
- [x] 验证非 Git、无提交、加载失败／重试和首次发送失败；不影响已创建任务的实际目录。
- [x] 运行相关类型检查、聚焦测试、真实 UI 与 Git 验证和 diff 审计；涉及会话代码时按 App 规则做性能对照。

## 同轮实施项

以下项目已纳入本轮授权；归档不会隐式合并，管理不会自动接管或回收外部目录：

- **占用覆盖：** 将手动终端／PTY、V2 执行纳入明确的占用合同；外部和脱离 Session 的进程需要单独说明检测能力与边界。
- **本地文件保留：** 区分可重建依赖／产物和需要保留的 Git ignored 文件，例如被忽略的 `.env`；本轮已补齐 ignored 文件保留。
- **历史及孤立目录：** 已补齐未绑定任务识别、旧目录接管和手动清理入口；不默认接管或删除外部目录。
- **管理可见性：** 集中展示目录、分支、关联任务、空间占用及清理状态，补齐失败原因和手动重试入口。
- **成果合回：** 专门的合回和冲突处理体验；保持与归档／回收职责分开，不在归档时隐式合并。
- **V2 生命周期接入：** 补齐任务菜单在 V2 下的能力门控、专用归档／恢复／删除接口和执行占用合同。

## 本轮实现合同与验收记录

### 占用与生命周期

- Core 增加可选目录租约接入口，未安装宿主适配时保持现有行为；Lab 同一后端的 V1、V2 和 PTY 共用 Worktree 生命周期占用表。
- PTY 按启动目录取得租约，关闭／退出／所属 Location 释放时归还；V2 在完整 Session drain 期间持有租约。覆盖 canonical 路径别名与目录包含关系。
- 检测限于本后端记录的占用。外部进程、终端启动后 `cd` 到别处、脱离后端生命周期的子进程不在检测范围内，不宣称全系统进程检测。
- V2 提供 capabilities 与专用 archive／restore／delete 接口；无宿主适配的独立 V2 服务明确报告不支持。UI 以实际能力门控，未知或失败不能当成支持。
- [x] 真实 HTTP／Git 验证 V2 创建、归档回收、目录缺失后的恢复和删除。
- [x] V1／V2 PTY 占用、pending 期间拒绝新终端、关闭后续归档，以及完整 V2 drain 的占用释放。
- [x] V2 前端所有入口能力门控、请求与缓存同步；浏览器回归覆盖不支持、能力失败重试和专用 mutation。

### 文件保留

- 默认保存 Git 可表达的暂存、未暂存、普通未跟踪文件和 ignored 本地内容；`.env`、本地数据库、证书、普通产物目录均不因名称被排除。
- 仅跳过明确可重建依赖／缓存，例如 `node_modules`、`.pnpm-store`、Python 缓存、`.yarn/cache` 与 `.next/cache`；服务层另支持显式保存全部 ignored 内容。
- 管理详情展示保留、跳过、无法保存的明细。符号链接只保存链接本身，不遍历目标；特殊文件、嵌套仓库或读取失败阻止回收并保留现场。
- Git stash 兼容的保存对象增加独立 ignored parent；兼容已有两／三 parent 保存；恢复保护同路径文件，删除目录前复查保存快照与当前文件一致。
- [x] 真实 Git 保存／恢复、ignored-only、空目录、碰撞、部分恢复重试、旧格式和 stash apply 兼容验证。
- [x] 包内 Node 路径同样覆盖本轮 ignored 内容保留。

### 管理与成果合回

- 项目菜单提供集中管理，按 Git 当前清单和后端生命周期记录展示目录、实际分支、关联任务、占用、回收／错误状态；空间与 ignored 明细按需读取。
- 未绑定目录提供显式接管／清理。接管不创建 Session、不改目录或分支；主目录、共享或不属于当前仓库的目录不能接管；接管已有用户分支不赋予删除该分支的所有权。
- Git 的主 worktree 以实际 Git 清单为准，不能把项目第一次打开的目录当成主 checkout。
- 合回先用独立临时 index 和 Git 三方合并计算差异／冲突，包含来源的已提交、未提交和普通未跟踪结果；ignored 内容不作为合回输入。
- 确认时校验来源 HEAD／tree 和目标 HEAD／merged tree 未变化，且两端无后端占用、目标干净且无进行中的 Git 操作。将结果放入主目录暂存区，不隐式提交或移动分支。
- 冲突展示具体文件和差异，逐文件显式选择主目录或 Worktree 的整份版本；不默认选边。选择后重新预览最终结果，确认失败保留预览和文件。过期预览、目标本地文件碰撞或数据无法完整读取时拒绝应用。选择与四个 Git 哈希由 reviewID 绑定，不能绕过二次预览。支持文本、二进制、符号链接、模式和修改／删除冲突；复杂目录／重命名冲突明确拒绝，保留原文件供现有 Git 工具处理。
- [x] 真实 Git 验证合回结果、三方冲突、过期预览、目标脏状态、ignored 文件碰撞和租约保护。
- [x] 管理／接管／清理／合回的真实 HTTP 和 UI 验证。
- [x] 代码与生成客户端一致性、包级类型检查、会话性能对照、Node 包验证及 diff 审计。
- [x] 提交本任务并按 DEVELOPMENT.md 合入本地 `dev`；不自动推送、发布或重启应用。

### 目录浏览与运行资源边界

- 本轮真实 UI 验证发现目录选择器通过项目文件接口逐级浏览绝对路径，使 `/` 和临时目录祖先被初始化为项目并启动 `fs-events` 监听；测试后端曾达到约 805% CPU。该实例已关闭。
- 新增鉴权下的 `GET /api/directory?path=...`，只列出一层文件／目录。处理器仅依赖全局 FSUtil，不进入 Instance、Location、插件、Core FileSystem、FFF 或文件监听。
- 两套选择器的目录树、路径补全和文件建议都使用该无状态入口。普通输入在当前层级筛选，显式路径逐层列举；已有项目仍按原列表搜索。不再为了目录选择启动递归索引。
- 旧服务器缺少此接口时显示请求失败，允许重试，不回退到会启动项目运行时的旧文件接口。
- [x] 单层目录、文件、符号链接、失败／重试与认证 transport 聚焦验证。
- [x] 界面项目选择回归验证只请求新目录接口；恢复请求通过 query 或 SDK 目录请求头正确路由。
- [x] 真实 UI 日志确认仅为已打开的小型测试项目建立实例，浏览祖先目录不产生额外监听。


### 验证记录（2026-09-07）

- App 四组浏览器回归共 35 条通过：任务工作台、输入保留、Worktree 管理和目录选择器近期项目搜索。覆盖 V2 能力缺失／重试、删除取消／失败重试、合回冲突选择后二次预览和失败保留。
- App、App E2E、Client、Core、Protocol、Server、OpenCode 的包级类型检查通过；PTY 与 Worktree 生命周期、保存恢复、管理、合回和目录浏览使用实际实现及隔离 Git／HTTP 测试验证。客户端由所属生成脚本更新。
- 同一生产构建 benchmark，各取三次冷／热切换：冷切换首次正确画面中位数 55.8 → 58.1ms，稳定画面 78.5 → 77.6ms；热切换首次正确画面 17.3 → 20.7ms，稳定画面 23.1 → 29.5ms。两轮空白和错误目标采样均为 0；热切换增加约 6ms，单次对照不足以归因为代码回归。
- 翻译沿用本项目词汇，并参考 Git 中文文档及 VS Code Git 文档；保留 Worktree、Git、PTY 等开发术语。
- 补充回归验证 Worktree 开关往返时，文字、图片和文件上下文完整保留；Project 的真实 Git alias 回归及完整 37 条测试通过。
- `httpapi-v2-pty.test.ts` 的既有 WebSocket 超时在干净基线也能复现；本轮独立 PTY 租约、V1／V2 HTTP 占用及关闭释放测试通过，不将基线失败计为本轮通过项。
- Home／会话命令能力查询首次失败后，支持其他入口重试成功同步恢复，以及窗口重新聚焦重试；真实 Solid resource 4 条测试、原兼容层 19 条测试及 App 类型检查通过。
- 任务候选包通过 Developer ID 签名校验；包内 Electron Node 实际执行无状态目录浏览、Worktree 创建、V2 能力查询、归档／恢复／删除、`.env` 保存和缓存排除、管理详情、合回主目录暂存及分支／引用清理，全部通过。此包用于任务验证，尚未安装为集成 Lab。

### 最终整合验收

- 本轮实现提交 `289076b3ef`；吸收本地 `dev` 的标题栏与侧工作区改动（`8a830bdbf2`），保留新版布局并补齐两套任务菜单的生命周期能力门控。
- 最终相关浏览器用例共 54 条通过：整轮 51 条通过，新增的三条测试在修正 fixture／行为断言后单独通过。覆盖跨项目非默认分支不串用、选项失败后阻止发送并保留输入、重试和本地继续、无提交仓库、输入附件及上下文、工作台、管理和侧工作区。
- 整合后生产 benchmark 冷切换首次正确画面／稳定画面中位数为 54.8／73.6ms，热切换为 19.1／22.5ms；空白与错误目标采样均为 0。最终类型检查和 diff 审计通过。
- 真实 UI 在独立 HOME／XDG 和小型 Git fixture 中完成：目录选择、开关往返、管理空间及 ignored 明细、普通与冲突合回、手动接管、清理取消／确认、任务归档／恢复。实际 Git 核对 HEAD 不变且仅预期结果进入主目录暂存区，ignored 配置保留、缓存不恢复、接管的用户分支不删除。浏览器 page error 为 0。
- macOS 别名身份从 Worktree 路径生成、创建响应、Project 清单到新输入均一致；新建结果实测为 canonical 路径，菜单仅一项并正确选中。
- 最终任务候选为 Lab #14，构建 `20260906.174132`，嵌入父提交 `5db2f49126` 和 dirty 标记。构建后比对 4724 个产品源码／清单文件哈希无变化，最后的 canonical 创建修复已在候选中实际验证。该候选通过签名及包内 Electron Node 全链路验证，未当作正式集成安装版交付。
- 隔离实例的日志只为 fixture 项目与 Worktree 创建 watcher，没有 `/`、HOME 或其祖先；全部自建浏览器与前后端已关闭。多次短浏览器连接期间出现过一次 GlobalBus 监听数告警，未据此宣称已证明或修复通用监听泄漏。
- 外部进程占用检测、复杂目录／重命名冲突的编辑解决、旧服务器缺少无状态目录接口等边界见上文；本轮没有定时回收、隐式提交／合并、远端推送、公开发布或应用重启。

# 开发阶段：多线开发与本地交付

**一条集成主线 `dev`，每个任务一个分支／worktree；完成后自动合线，需要体验时统一发 Lab。** 默认由 agent 完成这些步骤，用户不用逐次指挥 Git 命令。沿用 Sandy 的“先汇入主线，再统一交付”，按本仓实际脚本执行。

## 日常只分三件事

| 用户意图           | Agent 默认完成的范围                                                                                                                                        |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 实现／修复一个功能 | 独立开发、聚焦验证、只提交本任务改动、自动合入本地 `dev`，报告提交；不要求另说一次“提交”“合并”                                                              |
| 跑测试／发测试     | 在任务 worktree 验证，必要时启动隔离实例；不覆盖 `/Applications/OpenCode Lab.app`，不把单个分支的包当成集成版本                                             |
| 发 Lab／发 debug   | 完成本次范围内已就绪改动的合线，从干净 `dev` 构建、校验并安装 `/Applications/OpenCode Lab.app`；不推送远端，不发布 GitHub Release，不自动重启正在运行的应用 |

表中是给 agent 的自然语言指令，不是脚本名；“发 debug”在本仓等同于 Lab 本地交付，实际打包命令是 `bun run lab`，安装由 agent 接着完成。

“只分析”“先对齐”“先别提交／合并”等明确限制优先；独立答疑和诊断仍只读。单独说“提交”只提交本任务；单独说“合并到主线”以 `dev` 为目标。推送 `origin`、同步 `upstream`、公开发布仍需对应授权，已有授权不重复询问。

## 1. 多线开发：并行做，依次合

- **`dev` 是本项目主线。** `origin/dev` 是 fork 的远端，`upstream/dev` 是上游基线；本地合线不依赖远端在线。无需新增 `main`、长期 `develop` 或发布分支。
- **新任务从当前本地 `dev` 开短分支和独立 worktree。** 分支名遵守根 `AGENTS.md`，例如 `task-search`。一个工作目录只由一个任务负责；已有任务继续使用自己的目录。
- **任务完成就提交并合线，不攒一批手工补交。** 提交只包含该任务的文件／改动块；不执行 `git add -A` 收走其他人的工作，也不强制迁移已有脏改动。
- **合线顺序固定：** 在任务分支提交 → 将最新本地 `dev` merge 进任务分支 → 解决属于本任务且语义明确的冲突 → 验证合并结果 → 在主 checkout 用 `git merge --ff-only <任务分支>` 推进 `dev`。不 rebase 别人的分支，不在主线临时贴补丁打包。
- **主线更新串行。** 同一时刻由一个 agent 完成合线／本地交付；其他任务照常开发和测试。若 `dev` 在验证后前进，重新合入最新 `dev`，只复查受新增交集影响的内容，再尝试快进；不能强推指针绕过检查。
- **主 checkout 的合线和交付要求工作区干净。** 遇到既有脏改动，先查归属；保留已完成的任务提交，说明具体阻塞，不能 stash、reset 或顺手提交。主线推进后，不自动删除任务 worktree、分支或 Session。

新主线已经包含前一条线的成果，后一条线必须带上它再合入。例如 A 先完成，B 合线前先吸收包含 A 的 `dev`；用户下一次拿到的集成包应同时含 A、B，不会被 B 自己的旧分支快照覆盖。

## 2. 验证只做与改动有关的部分

- 开发时优先使用现有源码运行、热更新和包级聚焦测试，不为每次改动重打完整应用。
- 文档检查格式、链接和 diff；业务代码运行相关测试及适用的包级 `bun typecheck`；UI／行为问题还要核对对应的真实流程。目录 `AGENTS.md` 中已有的必要检查继续适用。
- 已通过的检查不为凑流程反复运行。吸收主线后有相关代码交集，或前一轮失败、证据失效时，才补做受影响检查。
- 用当前 checkout 的锁文件和工具版本。构建输出、生成文件和可写依赖不能在多个 worktree 间相互覆盖；不借“复用缓存”混用别的分支的 `out`／`dist`。
- 测试实例要有独立的用户目录、后端状态和可用端口，记录它的来源与连接地址。换端口不等于数据隔离；项目代码目录也不会因此变成沙箱。

本阶段不额外要求本地 PR、Release、版本标签、每个任务一份方案文档或全仓测试。需要说明的内容放在任务回报里：**提交、合线结果、实际验证、剩余阻塞**。

## 3. 本地交付：一次请求，完成构建与安装

1. **确定来源。** 本次需要交付的任务先按上述规则进入 `dev`。从主 checkout 的干净 `dev` 构建，记录完整 SHA；发包期间不并发推进它。未完成、未验收或不在范围内的分支留在自己的 worktree。
2. **先做候选包。** 依赖就绪后，从 `packages/desktop` 执行 `bun run lab`。它串联 `prebuild`、`build:lab` 和 `package:lab`，产物在该包的 `dist-lab/`。此阶段保持已安装应用和用户数据不动。
3. **核对候选。** 确认源码 HEAD／工作区未漂移，包的渠道为 `lab`，构建 ID 和 commit 对得上，必要资源齐全，签名有效且符合既有 Lab 身份。按本次功能核对要使用的后端路径，不能仅凭界面 commit 认定后端也包含修改。
4. **安装同一个候选。** 一个交付者操作目标，用独立 staging 完成复制和校验后再替换应用；保留原包直到新包校验完成，失败时恢复原包。禁止直接往运行中的目标目录逐文件覆盖。保留用户数据、认证、Session 与原有进程，不杀进程、不抢占端口、不自动重启。
   - 运行中的实例必须已持有独立资源快照：启动日志 `runtime resources prepared` 的 renderer 路径应位于该实例的 `opencode-runtime-*` 临时目录。仅把应用整体改名再替换，仍会让旧 Electron 的 ASAR 索引读到新包内容，不能视为运行安全。
   - 对尚未采用资源快照的旧实例，先完成候选构建与校验，再请用户退出旧应用后安装；安装新代码不能补救已经运行的旧代码。不能以“稍后重启”为由继续覆盖。
   - 快照包含 ASAR 与 unpacked 文件，供 renderer、preload 和本地 Node sidecar 使用；正常退出时等待本地服务停止后清理本实例快照。异常退出可能遗留临时目录，不扫描删除其他实例的目录。
   - Lab 候选包含本 fork 的 `opencode-lab`，同时用作独立后端和终端客户端。安装应用后，从干净主 checkout 的 `packages/desktop` 执行 `bun run install:lab-cli "/Applications/OpenCode Lab.app/Contents/Resources/opencode-lab"`，将同包 CLI 原子安装到 `~/.opencode/bin/` 并接入 `~/.local/bin/opencode-lab`。保留官方 `opencode`；遇到已有同名独立命令则报告冲突，不覆盖。独立 CLI 修复可先单独构建、验证和安装；安装不会替换已运行的共享后端。后端变更须在用户明确停止旧后端后再启动验证，不自动重启。
   - Lab 客户端退出只断开连接，后端从不可变版本路径继续运行。验收用 `opencode-lab backend status` 和桌面启动日志核对实际 PID、版本、协议；兼容协议不代表已经运行本次新代码。首次升级拒绝仍打开旧数据库的进程，完成协议升级后旧包不可回退写同一 profile，不回滚用户数据来绕过检查。
5. **报告准确状态。** 给出构建 ID、来源 SHA、安装位置和验证结果。应用原来在运行时，说明“新包已安装，当前运行实例仍待用户重启验证”；用户重启后，再只读核对实际进程、构建身份和后端。回退应用包不等于回退数据库，不自动回滚用户数据。

Lab 序号从 `#1` 开始，每次 `build:lab` 分配一个新号，显示在版本／构建信息中。同一仓库各 worktree 共用 Git 元数据目录中的计数；开发热更新不占号，构建失败后已分配的号不复用。重新安装同一个候选包沿用原号。

合线和安装串行只是防止互相覆盖；各任务的编辑、测试继续并行。多个请求连续到达时，尚未开始的交付可合并为一次最新 `dev` 构建；不取消已经开始的安装事务，也不替用户收走仍在开发的功能。

## 4. 现有工具能做什么

这里的“自动”首先指 agent 按约定完成已有 Git 和构建步骤，不要求先建一个发布服务。下面的差别必须保留，不能把流程约定写成脚本已实现的能力。

| 现有入口                                                                                               | 实际能力／限制                                                                                                                                               |
| ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [Desktop package.json](./packages/desktop/package.json) 的 `bun run lab`                               | 构建并生成 macOS 目录包；**不安装、不启动应用**。安装仍需交付 agent 完成上述步骤                                                                             |
| [electron.vite.config.ts](./packages/desktop/electron.vite.config.ts)                                  | 写入时间构建 ID、版本、Lab 递增序号、短 commit、dirty 和构建时间；先沿用这些字段，不额外维护手工版本号。dirty 标志是信息，不是自动拒绝打包的门禁             |
| [prebuild.ts](./packages/desktop/scripts/prebuild.ts)、[utils.ts](./packages/desktop/scripts/utils.ts) | 构建本仓 Node 后端、Lab CLI 和固定版本 V2 CLI。Lab 统一使用同包 `opencode-lab backend serve`，不通过 V2 开关启动第二个 profile owner；其他渠道保持原 sidecar 选择。 |
| [Lab 环境隔离](./packages/desktop/src/main/lab-environment.ts)                                         | 隔离 Lab 与其他渠道的应用／后端状态；多个同身份 Lab 并不因此各有独立数据                                                                                     |
| [Desktop 启动入口](./packages/desktop/src/main/index.ts)                                               | `OPENCODE_TEST_ONBOARDING=1` 是临时 onboarding 测试模式，不是通用 worktree 开发配置；尚无通用的多实例隔离启动命令，不能直接复用正式 Lab profile 代替隔离测试 |

当前没有 Lab 专用的自动合线器、交付锁、安装事务脚本或构建回执系统。由一个 agent 串行交付并记录上述关键事实即可起步；如果多个独立 agent 无法协调同一安装目标，先保留候选包，不能互相覆盖。后续只补实际阻碍迭代的最小脚本，不把规则升级成一套发布平台。

## 5. 何时停，何时继续

代码已验证、归属清楚、可正常合线时直接继续，不以“等待用户确认提交／合并”收尾。测试失败先在任务分支修；只有以下情况才保留进度并请求必要输入：跨任务冲突的产品语义不明确、主 checkout 混有他人工作、要扩大范围或执行尚未授权的外部操作、真实环境阻塞无法自行排除。

本规则从后续实施任务采用；已有在途改动按原授权和归属收尾，不因启用新流程被自动打包、提交或搬走。首次正式按此流程发 Lab 前，需要先将现有主 checkout 的在途工作收束到可追溯提交。

参考：Sandy 的 `AGENTS.md`、`scripts/build-darwin-debug-app.sh` 与 `sandy-debug-iteration/SKILL.md`。借用的是主线集成和候选包交付方法；分支名、包结构、端口及实例管理以本仓为准。
